spring.application.name=Backend

# PostgreSQL Database Configuration (Digital Ocean Managed PostgreSQL)
spring.datasource.url=jdbc:postgresql://db-postgresql-blr1-97829-do-user-37647391-0.k.db.ondigitalocean.com:25060/defaultdb?sslmode=require
spring.datasource.username=doadmin
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=IPh0QiYq8RN+40oPeT2Yy6jxTuewduD/DWUCnWF5Dcw=
jwt.expiration=86400000

# AWS S3 Configuration
aws.s3.bucket-name=myclassesimages
aws.s3.region=ap-south-1
aws.s3.access-key=${AWS_ACCESS_KEY}
aws.s3.secret-key=${AWS_SECRET_KEY}

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Credits System Configuration
credits.lead-cost-per-lead=1
credits.featured-cost-per-month=500
credits.featured-duration-days=30
credits.rupee-per-token=10
